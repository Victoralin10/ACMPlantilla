a ^ phi(n) = 1 mod n  iff (if and only if)  n and a are coprimes

Bonus:

let n = p1 ^ a1 * p2 ^ a2 ...

phi(n) = (p1 - 1) * p1 ^ (a1 - 1) * (p2 - 1) * p2 ^ (a2 - 1) ...

phi(n) = n * (for each distinct prime 'p' that divides n: the product of (1 - 1 / p))